"""Spiking neural network arm - stretch goal (CLAUDE.md §8).

snnTorch LIF neurons with surrogate gradients on the Conv3d backbone, plus the
ANN->SNN conversion path from the trained 3D CNN. Accuracy alone will never justify
an SNN; report spike counts / SynOps and an energy estimate alongside AUC.
"""
